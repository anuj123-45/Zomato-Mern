import React from 'react'

export const PrevArrow = (props) => {
    return <div className={props.className} style={{ ...props.style, backgroundColor: "#494a4c", borderRadius: "50%" }} onClick={props.onClick}>

    </div>
}

export const NextArrow = (props) => {
    return <div className={props.className} style={{ ...props.style, backgroundColor: "#494a4c", borderRadius: "50%" }} onClick={props.onClick}>

    </div>
}

